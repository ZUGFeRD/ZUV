<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
	<groupId>org.zugferd</groupId>
	<artifactId>ZUV</artifactId>
	<packaging>jar</packaging>
	<version>0.3.0-SNAPSHOT</version>

	<repositories>
		<repository>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<id>vera-dev</id>
			<name>Vera development</name>
			<url>http://artifactory.opf-labs.org/artifactory/vera-dev</url>
		</repository>
	</repositories>

	<dependencies>

		<!-- Verapdf -->
		<dependency>
			<groupId>org.verapdf</groupId>
			<artifactId>core</artifactId>
			<version><!--  [1.1.0,1.2.0)-->1.6.2</version>
		</dependency>
		<!-- Schematron -->
		<dependency>
			<groupId>com.helger</groupId>
			<artifactId>ph-schematron</artifactId>
			<version>4.3.1</version>
		</dependency>

		<!-- Apparently a hidden (runtime) ph-schematron(?) dependency -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.12</version>
		</dependency>

		<!-- Apparently another hidden (runtime) ph-schematron(?) dependency -->
		<dependency>
			<groupId>org.hamcrest</groupId>
			<artifactId>hamcrest-all</artifactId>
			<version>1.3</version>
		</dependency>
		<!-- Apparently another hidden (runtime) ph-schematron(?) dependency -->

		<dependency>
			<groupId>xml-resolver</groupId>
			<artifactId>xml-resolver</artifactId>
			<version>1.2</version>
		</dependency>
	</dependencies>
	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
		</resources>
		<plugins>
<!--  

JS@2017-07-09 comment 1/3: (2 and 3/3 are in the ZUGFeRDExtractor.java) 
I want to remove this comment. This one goes....	

			<plugin>
				<configuration>
					<minimizeJar>true</minimizeJar>
					<filters>
						<filter>
							<artifact>*:*</artifact>
							<excludes>
								<exclude>META-INF/*.SF</exclude>
								<exclude>META-INF/*.DSA</exclude>
								<exclude>META-INF/*.RSA</exclude>
							</excludes>
							<includes>
								<include>net/sf/saxon/**</include>
								<include>org/apache/xml/resolver/helpers/**</include>
							</includes>

						</filter>
						<filter>
							<artifact>com.helger:*</artifact>
							<includes>
								<include>com/helger/schematron/**</include>
								<include>com/helger/commons/**</include>
							</includes>
						</filter>
						<filter>
							<artifact>junit:junit</artifact>
							<includes>
								<include>junit/framework/**</include>
								<include>org/junit/**</include>
							</includes>
							<excludes>
								<exclude>org/junit/experimental/**</exclude>
							</excludes>
						</filter>
						<filter>
							<artifact>org.hamcrest:hamcrest-all</artifact>
							<includes>
								<include>org/hamcrest/*</include>
							</includes>
						</filter>

						<filter>
							<artifact>org.verapdf:*</artifact>
							<excludes>
								<exclude>**</exclude>

								<exclude>org/verapdf/core/*</exclude>
								<exclude>org/verapdf/core/*</exclude>
								<exclude>org/verapdf/features/*</exclude>
								<exclude>org/verapdf/metadata/*</exclude>
								<exclude>org/verapdf/pdfa/*</exclude>
								<exclude>org/verapdf/policy/*</exclude>
								<exclude>org/verapdf/release/*</exclude>
								<exclude>org/verapdf/report/*</exclude>
								<exclude>org/verapdf/processor/*</exclude>
							</excludes>
						</filter>
					</filters>
				</configuration>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.0.0</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

... up to here

But if I do I will get  a
Jul 09, 2017 11:53:16 AM org.verapdf.processor.ProcessorImpl extractFeatures
WARNUNG: Exception caught when extracting features of item
java.lang.NullPointerException
	at org.verapdf.processor.FeaturesPluginsLoader.initializeExtractor(FeaturesPluginsLoader.java:132)
	...
	:-(((

-->

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<encoding>UTF-8</encoding>
					<!-- <archive> <manifest> <addClasspath>true</addClasspath> <mainClass>ZUV.Main</mainClass> 
						</manifest> </archive> -->
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
					<!-- http://stackoverflow.com/questions/574594/how-can-i-create-an-executable-jar-with-dependencies-using-maven 
						mvn clean compile assembly:single -->
					<!-- or whatever version you use -->
					<source>1.7</source>
					<target>1.7</target>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
